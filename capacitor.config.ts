import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kvo.app',
  appName: 'kvo2022',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
